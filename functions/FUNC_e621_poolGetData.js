module.exports.run = async (message) => {
  // check if pool is present (isPool)
  // get current page (currentPage)
  // return list of all posts (array: all posts)
};

module.exports.help = {
  name: 'FUNC_e621_poolGetData',
};
